// id, user_id, house_no, apartment_name, street, landmark,
//  area, city, pincode, nick_name,
//   is_default, deleted_at, created_at, updated_at

const mongoose = require('mongoose');

let user_addresses = mongoose.Schema({
    
})